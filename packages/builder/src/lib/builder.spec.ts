import { build } from './builder';

describe('builder', () => {
  it('should work', () => {
    // build("C:\\Users\\matbo\\Documents\\le-static\\test\\simple");
    // TODO: Checks that the generated dist file is right
    expect("builder").toEqual('builder');
  });
});
